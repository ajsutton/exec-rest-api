"""Integration-test fixtures.

The `anvil_url` and `proxy_client` fixtures are defined at the top-level
`tests/conftest.py` so they can be shared between the integration and
conformance suites. This file is kept for any integration-only fixtures
that may be added later.
"""
